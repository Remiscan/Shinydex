{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "deno run --allow-run --allow-read --allow-write=./modules build/auto.build.js",
      "label": "[auto] compile to JS on file change",
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}